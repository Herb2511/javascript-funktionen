var msg = 'Melden Sie sich für unseren Newsletter an, um 10% Rabatt zu erhalten!';

function updateMessage() {
var ell = document.getElementById('message-travel');
ell.textContent = msg;
};
// updateMessage();

console.log(msg);